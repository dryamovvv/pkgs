name: Build and Deploy

on:
  push:
    branches: [main]
    paths:
      - "packages/**"
      - "ci/**"
      - ".github/workflows/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: set-matrix
        run: echo "matrix=$(ci/detect-changes.sh)" >> $GITHUB_OUTPUT

  build:
    needs: detect
    if: ${{ needs.detect.outputs.matrix != '[]' && needs.detect.outputs.matrix != '' }}
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        pkg: ${{ fromJSON(needs.detect.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v4

      # ── Docker image cache ──
      - name: Cache Docker image
        id: docker-cache
        uses: actions/cache@v4
        with:
          path: /tmp/docker-image.tar
          key: docker-archlinuxarm-v2-${{ hashFiles('.github/workflows/build.yml') }}

      - name: Load / pull Docker image
        run: |
          if [ -f /tmp/docker-image.tar ]; then
            docker load -i /tmp/docker-image.tar
          fi
          docker pull lfdevs/archlinuxarm:base-devel
          docker save lfdevs/archlinuxarm:base-devel -o /tmp/docker-image.tar

      # ── Pacman package cache ──
      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /tmp/pacman-cache
          key: pacman-cache-v2-${{ hashFiles('packages/*/PKGBUILD') }}
          restore-keys: pacman-cache-v2-

      # ── ccache ──
      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /tmp/ccache
          key: ccache-v2-${{ matrix.pkg }}-${{ github.run_id }}
          restore-keys: ccache-v2-${{ matrix.pkg }}-

      # ── Build ──
      - name: Build in container
        run: |
          mkdir -p /tmp/pacman-cache /tmp/ccache
          docker rm -f builder 2>/dev/null || true
          docker run -d --name builder --privileged \
            -v /tmp/pacman-cache:/var/cache/pacman/pkg \
            -v /tmp/ccache:/ccache \
            lfdevs/archlinuxarm:base-devel sleep infinity
          docker cp . builder:/workspace/
          docker exec builder bash -c "cd /workspace && ci/build-package.sh ${{ matrix.pkg }}"

      - name: List packages
        run: |
          docker cp builder:/workspace/packages ./packages-built
          find packages-built -name '*.pkg.tar.*' -type f -ls

      # ── Deploy (inside container, retry-based) ──
      - name: Deploy to repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          docker exec -e GITHUB_TOKEN builder bash -c "cd /workspace && ci/deploy-package.sh packages/${{ matrix.pkg }}"

      # ── Artifact backup ──
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.pkg }}
          path: packages-built/${{ matrix.pkg }}/*.pkg.tar.*
          if-no-files-found: error

      # ── Cleanup ──
      - name: Stop container
        if: always()
        run: docker rm -f builder 2>/dev/null || true
